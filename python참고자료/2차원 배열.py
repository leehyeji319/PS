# 행 row 열 column
columns = 3
rows = 2
graph = [[0] * columns for i in range(rows)]
graph1 = [[] * columns for i in range(rows)] #빈배열 생성


# 2차원 배열에서 인덱스 구하기 2행 10열 일때
newlist=[(i,j) for i in range(2) for j in range(10) if keyboard[i][j]==find_list[x]]